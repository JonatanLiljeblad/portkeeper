apiVersion: mcp.portkeeper.dev/v1alpha1
kind: MCPServer
metadata:
  name: runbooks
  namespace: portkeeper-demo
spec:
  allowedServiceAccounts:
    - namespace: portkeeper-demo
      name: mcp-demo-client
  image: portkeeper/runbooks:e2e
  port: 9001
  tools:
    - read_runbook
  authType: none
